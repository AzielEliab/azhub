# AZHub

Public identity: **Aziel Eliab** only.

This is SPACE / Blank Key (AIH-WP-1.0) — a neutral spatial container.
It is **not** AZInterface. Never collapse Hub into Interface.

AZInterface is a sibling: https://github.com/AzielEliab/azinterface

**Dual surface is mandatory.** Human Worker canvas stays complete
(drag-drop + drop popup). Backend MCP / OpenAPI / FragGate ops stay
first-class. Do not ship UI-only.

Agent path is FragGate only:
`POST https://aziel-runtime.vibelock.workers.dev/v1/fraggate/call`
with `slug=azhub`. Catalog MCP:
`POST https://aziel-runtime.vibelock.workers.dev/mcp`.
This Worker's `/mcp` is a pointer, not a second MCP. Human chrome uses
`/v1/{op}` (single-segment local ops). `/v1/fraggate/*` and
`/v1/runtime/*` PROXY to aziel-runtime — they are not local op names.
Parent wires the engine later.

Blank Key: geometry without intent. Co-presence is inert. Declared
wiring only. No ranking, intent, completeness detector, or Snap
activation. Hub does not unlock modules.

Forks are welcome and always allowed. Apache-2.0.
