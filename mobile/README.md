# AZHub — iPhone & Android

On-device Blank Key: place tiles, declare tethers, isolate.
Not AZInterface. Offline. No analytics. Dark matte / gold.

Application id: `com.azieeliab.azhub`

## Open in Android Studio / Xcode

The `android/` and `ios/` folders here are skeleton READMEs because
this tree was written without the Flutter SDK on PATH.

```bash
cd mobile
flutter create --org com.azieeliab --project-name azhub .
flutter pub get
flutter run
```

Then open `android/` in Android Studio, or `ios/Runner.xcworkspace` in
Xcode.

## Honest scope

Blank Key / AIH-WP-1.0. Not a store. Not a recommender.

## Desktop package (counted download)

This phone app does not replace the desktop package.

# → https://azhub-download-tracker.vibelock.workers.dev/ ←

GitHub: https://github.com/AzielEliab/azhub

**Forks are welcome and always allowed.**
